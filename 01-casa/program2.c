#include <stdio.h>
#include <stdlib.h>
#define TAM 5

int main(){

    int matriz[TAM][TAM];
    int i, j;

    for (i = 0; i < TAM; I++){
        printf("matriz[i][i] =  ", i);
        scanf("%d", &matriz[i][j]);
    }

    for (j = 0; j < TAM; j++){
        printf("matriz[j][j] = %d%d", j);
        scanf("%d", &matriz[i][j]);
    }

    return 0;
}